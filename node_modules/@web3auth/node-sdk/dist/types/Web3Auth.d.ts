import { IProvider } from "@web3auth/base";
import { IWeb3Auth, LoginParams, PrivateKeyProvider, Web3AuthOptions } from "./interface";
declare class Web3Auth implements IWeb3Auth {
    connected: boolean;
    readonly options: Web3AuthOptions;
    private torusUtils;
    private nodeDetailManager;
    private privKeyProvider;
    private currentChainNamespace;
    constructor(options: Web3AuthOptions);
    get provider(): IProvider | null;
    init({ provider }: {
        provider: PrivateKeyProvider;
    }): void;
    connect(loginParams: LoginParams): Promise<IProvider | null>;
}
export default Web3Auth;
